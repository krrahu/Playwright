 import {test,expect} from "@playwright/test";
 //syntax
 test("verfy the title",()=>{
    console.log('sample test')
 }
 )

 // to run this --> npx playwright test 

 // not need to define browser name like webdriver driver = new Chromedriver(); automatically it take browser deatils fron config file and it will launch 
 // is faster than selenium
 // 

